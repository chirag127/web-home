---
import PackageCard from '~/components/PackageCard.astro'
/*
 * /packages — the @chirag127/* npm package catalog, grouped by the 5
 * sidebar groups used in packages.oriz.in:
 *
 *   - Astro foundation
 *   - UI & widgets
 *   - Data & auth
 *   - Distribution
 *   - Testing
 *
 * Each card links to packages.oriz.in/<slug> for full docs. The catalog
 * is read from the 22-package memory note (see ~/.claude/projects/.../
 * twenty-two-packages-on-npm.md) plus any future packages added by the
 * agents-md-sync flow.
 */
import BaseLayout from '~/layouts/BaseLayout.astro'

type PkgGroup = 'astro' | 'ui' | 'data' | 'dist' | 'testing'

interface Pkg {
  brand: string
  slug: string // npm name without scope
  npmName: string // full @chirag127/<name>
  repo: string // chirag127/<repo-slug>-npm-pkg
  oneLine: string
  group: PkgGroup
}

const PACKAGES: Pkg[] = [
  // ── Astro foundation ────────────────────────────────────────────────
  {
    brand: 'astro-shell',
    slug: 'astro-shell',
    npmName: '@chirag127/astro-shell',
    repo: 'chirag127/astro-shell-npm-pkg',
    oneLine: 'Family-anchor Astro layout — header, footer, JSON-LD, feed discovery.',
    group: 'astro',
  },
  {
    brand: 'astro-test-utils',
    slug: 'astro-test-utils',
    npmName: '@chirag127/astro-test-utils',
    repo: 'chirag127/astro-test-utils-npm-pkg',
    oneLine: 'Vitest helpers for Astro components — happy-dom containers, slot fakes.',
    group: 'astro',
  },
  {
    brand: 'astro-content-helpers',
    slug: 'astro-content-helpers',
    npmName: '@chirag127/astro-content-helpers',
    repo: 'chirag127/astro-content-helpers-npm-pkg',
    oneLine: 'Content-collection schemas + loaders shared across reading sites.',
    group: 'astro',
  },

  // ── UI & widgets ────────────────────────────────────────────────────
  {
    brand: 'ui',
    slug: 'ui',
    npmName: '@chirag127/ui',
    repo: 'chirag127/ui-npm-pkg',
    oneLine: 'Broadsheet design-system primitives — typography, tokens, components.',
    group: 'ui',
  },
  {
    brand: 'multisearch',
    slug: 'multisearch',
    npmName: '@chirag127/multisearch',
    repo: 'chirag127/multisearch-npm-pkg',
    oneLine: 'Federated search popover — DuckDuckGo, Brave, Kagi, Google in one keystroke.',
    group: 'ui',
  },
  {
    brand: 'status-banner',
    slug: 'status-banner',
    npmName: '@chirag127/status-banner',
    repo: 'chirag127/status-banner-npm-pkg',
    oneLine: 'Family-wide status banner — fetches incident state at the edge, no flash.',
    group: 'ui',
  },
  {
    brand: 'consent',
    slug: 'consent',
    npmName: '@chirag127/consent',
    repo: 'chirag127/consent-npm-pkg',
    oneLine: 'Klaro-backed cookie consent banner with sane family defaults.',
    group: 'ui',
  },

  // ── Data ────────────────────────────────────────────────────────────
  {
    brand: 'feeds',
    slug: 'feeds',
    npmName: '@chirag127/feeds',
    repo: 'chirag127/feeds-npm-pkg',
    oneLine: 'RSS + Atom + JSON Feed builders — three formats from one source.',
    group: 'data',
  },
  {
    brand: 'jsonld',
    slug: 'jsonld',
    npmName: '@chirag127/jsonld',
    repo: 'chirag127/jsonld-npm-pkg',
    oneLine: 'Schema.org JSON-LD blocks — Organization, WebSite, Person, Article.',
    group: 'data',
  },

  // ── Distribution ────────────────────────────────────────────────────
  {
    brand: 'pwa',
    slug: 'pwa',
    npmName: '@chirag127/pwa',
    repo: 'chirag127/pwa-npm-pkg',
    oneLine: 'PWA wiring — manifest, service worker, install prompt, update toast.',
    group: 'dist',
  },
  {
    brand: 'pwabuilder',
    slug: 'pwabuilder',
    npmName: '@chirag127/pwabuilder',
    repo: 'chirag127/pwabuilder-npm-pkg',
    oneLine: 'CLI thin wrapper around PWABuilder — generates AAB/MSIX/iOS-project.',
    group: 'dist',
  },
  {
    brand: 'deploy',
    slug: 'deploy',
    npmName: '@chirag127/deploy',
    repo: 'chirag127/deploy-npm-pkg',
    oneLine: 'Cloudflare Pages deploy preset — wrangler config, env-var schema, CI matrix.',
    group: 'dist',
  },

  // ── Testing ─────────────────────────────────────────────────────────
  {
    brand: 'playwright',
    slug: 'playwright',
    npmName: '@chirag127/playwright',
    repo: 'chirag127/playwright-npm-pkg',
    oneLine: 'Playwright base config + fixtures shared across the family.',
    group: 'testing',
  },
  {
    brand: 'vitest',
    slug: 'vitest',
    npmName: '@chirag127/vitest',
    repo: 'chirag127/vitest-npm-pkg',
    oneLine: 'Vitest preset — happy-dom, V8 coverage, flavoured snapshot serializer.',
    group: 'testing',
  },
]

const GROUPS: { id: PkgGroup; label: string }[] = [
  { id: 'astro', label: 'Astro foundation' },
  { id: 'ui', label: 'UI & widgets' },
  { id: 'data', label: 'Data' },
  { id: 'dist', label: 'Distribution' },
  { id: 'testing', label: 'Testing' },
]
---

<BaseLayout
  title="Packages — chirag127"
  description="Free, open-source npm packages by Chirag Singhal — the foundations of the family. Astro layouts, UI primitives, data helpers, distribution and testing presets."
  wordmarkSubtitle="packages"
>
  <article class="page spine">
    <h1 class="page-title">Packages</h1>
    <p class="page-lede">
      The npm packages that power the family, grouped by
      purpose. Every card links to the full docs at packages.oriz.in.
    </p>

    {GROUPS.map((g) => {
      const items = PACKAGES.filter((p) => p.group === g.id)
      if (items.length === 0) return null
      return (
        <section class="pkg-section">
          <h2 class="section-h">{g.label} <span class="section-count">({items.length})</span></h2>
          <div class="pkg-grid">
            {items.map((p) => (
              <PackageCard
                brand={p.brand}
                oneLine={p.oneLine}
                slug={p.slug}
                npmName={p.npmName}
                repo={p.repo}
              />
            ))}
          </div>
        </section>
      )
    })}
  </article>
</BaseLayout>

<style>
  .page {
    padding-block: clamp(2rem, 5vw, 3rem) clamp(3rem, 6vw, 4rem);
  }
  .page-title {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: clamp(2rem, 5vw, 2.75rem);
    margin: 0 0 0.5rem;
    color: var(--ink);
  }
  .page-lede {
    font-family: var(--font-display);
    font-size: 1.0625rem;
    color: var(--ink);
    max-width: 60ch;
    margin: 0 0 2rem;
  }
  .pkg-section {
    margin-top: 2.5rem;
  }
  .section-h {
    font-family: var(--font-mono);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--ink-mute);
    font-weight: 500;
    margin: 0 0 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--rule);
  }
  .section-count {
    color: var(--ink-mute);
    margin-left: 0.5em;
  }
  .pkg-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1rem;
  }
</style>
